NODE_ENV=production
SESSION_SECRET=BrntsDMS2025!uPbRaNtAs@SecR3t#PLNnp

# ── PERSISTENT DATA (WAJIB di production) ──
# Arahkan ke folder DI LUAR direktori app/git agar database TIDAK hilang saat deploy.
# Contoh Hostinger (ganti <user> dengan username akun, mis. u123456789):
#   BDMS_DATA_DIR=/home/<user>/bdms-data
#   BDMS_UPLOADS_DIR=/home/<user>/bdms-uploads
# Jika kosong, default ke ./database dan ./uploads (hanya untuk pengembangan lokal).
BDMS_DATA_DIR=
BDMS_UPLOADS_DIR=
